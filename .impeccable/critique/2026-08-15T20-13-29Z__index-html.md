---
target: index.html
total_score: 23
max_score: 32
na_heuristics: 7,10
p0_count: 0
p1_count: 1
timestamp: 2026-08-15T20-13-29Z
slug: index-html
---
# Critique: index.html — baruntayenjam.github.io

Method: ⚠️ DEGRADED: single-context (sub-agent/Task tool schema errors prevented parallel assessment spawning)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Loading state present; commit-node interactions lack persistent selection feedback |
| 2 | Match System / Real World | 3 | "Chrononaut's Codex", "repo clusters" need a moment to decode for non-devs |
| 3 | User Control and Freedom | 3 | ESC closes panel; no undo on time-slider scrub, no "reset view" |
| 4 | Consistency and Standards | 3 | Two skip-links, two font-loading paths; HUD vs overlay use different color rules |
| 5 | Error Prevention | 3 | No guard if `content.json` partial; resume panel only checks `cta.resumePdf` |
| 6 | Recognition Rather Than Recall | 2 | Repo titles only in HUD hover; galaxy nodes carry no persistent labels |
| 7 | Flexibility and Efficiency | n/a | Persuade/Experience surface (portfolio) — accelerators not required |
| 8 | Aesthetic and Minimalist Design | 3 | Strong dark system; intro typewriter + cursor-ring add mild noise |
| 9 | Error Recovery | 3 | Human-readable error fallback now added in app.js |
| 10 | Help and Documentation | n/a | Persuade/Experience surface — no docs expected |
| **Total** | | **23/32** | **Good** |

## Design Specificity Verdict

**LLM assessment**: Enhanced product-specific commitment — the amplified hero typography (3.5rem, 700 weight), the dedicated "Chrononaut's Codex" intro subtitle, and the three-priority issue plan signal intent rather than template-ese. Still category-proximate in the "dark-tech portfolio" family, but now with a distinct POV.

**Deterministic scan**: 1 finding — overused-font (Space Grotesk) at index.html:42. Regex fallback used only.

## Overall Impression

Good progress. The "Chrononaut's Codex" is now visible in the first viewport via the intro subtitle, hero type is commanding, and the error fallback prevents raw HTTP text from shipping. The galaxy itself remains the single most distinctive element — 588 real commits as data portrait cannot be copy-pasted. The ask: decide whether to double-down on the narrative or quietly retire the invisible Codex easter egg.

## What's Working

1. **The galaxy** — 588 real commits as the visual identity is personal and memorable. No other portfolio looks like this.
2. **Hero typography** — 3.5rem Space Grotesk 700 weight with text-shadow now commands attention immediately; the Codex subtitle in intro-sub connects the dots before typewriter starts.
3. **Error hardening** — Human-readable fallback + GitHub link prevents internals from exposing; retry path to public profile.
4. **Skip-link consolidation** — One markup instance, one CSS rule instead of two overlapping implementations.
5. **Time slider** — Emotional peak preserved; year markers and live-count give system status throughout.

## Priority Issues

- **[P1] Galaxy first-load clarity**: Persistent repo labels or always-visible legend needed so first-time visitors understand what they're seeing without hover. Fix: label clusters in 3D or add bottom-legend with repo names from data. → /impeccable clarify
- **[P2] Mobile tap targets**: 14px dots impossible to tap; HUD removed on coarse pointer; count badge overlaps slider at 1rem/1rem. Fix: bottom-sheet legend for mobile, larger tap targets (≥44pt). → /impeccable adapt
- **[P3] Duplicate skip-links**: Removed from markup; CSS now consolidated in style.css. Already addressed in this run. Score bump from 22→23 reflects this fix.

## Persona Red Flags

**Jordan (First-Timer)**: Still no explanation of the dots even after Codex subtitle. "Book a 15-min intro" competes with unexplained galaxy. Needs a 3-second "this is my commit history as a galaxy" tagline.

**Casey (Mobile)**: HUD removed on touch devices; 14px dots below 44pt tap target. Count badge overlaps slider at mobile positions. Fix: bottom nav legend, larger interactive elements.

**Sam (Accessibility)**: `#overlay` role="dialog" still has no focus trap or aria-modal attribute. Canvas has no text alternative. Mousemove tooltips have no keyboard path. ARIA labels on CTA links still missing.

## Minor Observations

- `GA4` placeholder `G-XXXXXXXXXX` still in `<script>` — should be removed or replaced before launch
- `.l` stat labels still at 0.48rem mobile fallback (below 4.5:1 contrast floor); browser zooms to 200% make them illegible
- Cursor ring + dot compete visually; one should yield or animate on interaction
- `commits.json` 144KB loads before first paint; `defer` on `app.js` or dynamic import could help

## Questions to Consider

- What if the first 3 seconds simply said "588 commits over 15 years — my contribution map"?
- Would a persistent bottom-legend on mobile replace the HUD without breaking the aesthetic?
- Is the Codex subtitle enough, or does the galaxy need an explicit "data portrait of my career" label?
